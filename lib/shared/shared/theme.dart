import 'package:flutter/material.dart';

ThemeData generalTheme = ThemeData(
  primaryColor: const Color(0xFF0b512d),
  colorScheme: ColorScheme.fromSeed(seedColor: const Color(0xff2e7d32), brightness: Brightness.dark),
);
